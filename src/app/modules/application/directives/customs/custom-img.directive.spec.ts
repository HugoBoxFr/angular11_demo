import { CustomImgDirective } from './custom-img.directive';

describe('CustomImgDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomImgDirective();
    expect(directive).toBeTruthy();
  });
});
