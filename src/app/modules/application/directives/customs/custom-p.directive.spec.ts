import { CustomPDirective } from './custom-p.directive';

describe('CustomPDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomPDirective();
    expect(directive).toBeTruthy();
  });
});
